{
  "name": "superhyre-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/dev.mjs",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "build": "npm --prefix auth-app run build"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
